# 우리 여행 지도 — 환경변수 예시
#
# 현재 이 프로젝트는 필수 환경변수가 없습니다.
# 데이터는 별도 API 키 없이 프로젝트 루트의 data/ 폴더(SQLite + 업로드 사진)에 저장됩니다.
#
# 환경변수가 필요해지면 아래처럼 추가하고, 실제 값은 .env.local에만 넣으세요.
# (.env.local은 .gitignore에 의해 커밋되지 않습니다)
#
# 예) NEXT_PUBLIC_EXAMPLE_KEY=
